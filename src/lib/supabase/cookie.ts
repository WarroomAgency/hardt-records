// One cookie name for every Supabase client (browser, server, proxy). @supabase/ssr
// otherwise derives the name from the URL's hostname, and the browser client uses a
// same-origin proxy URL while the server talks to Supabase directly.
export const COOKIE_NAME = "sb-hardt-auth";
